spring:
  application:
    name: expense-tracking
  datasource:
    url: jdbc:mysql://localhost:3306/expense_tracking?useSSL=false&serverTimezone=UTC&userPassword=NO
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
