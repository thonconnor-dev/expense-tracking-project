<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="60">

    <Properties>
        <Property name="log.pattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} -
            msg=%msg
            %X%n%throwable</Property>
        <Property name="log.dir">logs</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log.pattern}" />
        </Console>

        <RollingFile name="RollingFile" fileName="${log.dir}/app.log"
            filePattern="${log.dir}/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${log.pattern}" />
            <Policies>
                <TimeBasedTriggeringPolicy interval="1" />
                <SizeBasedTriggeringPolicy size="10 MB" />
            </Policies>
            <DefaultRolloverStrategy max="10" />
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="Console" />
            <!-- <AppenderRef ref="RollingFile" /> -->
        </Root>

        <!-- Example of a package-specific logger -->
        <Logger name="com.thonconnor.practice" level="info" additivity="false">
            <!-- <AppenderRef ref="RollingFile" /> -->
            <AppenderRef ref="Console" />
        </Logger>
    </Loggers>

</Configuration>